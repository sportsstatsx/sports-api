# service/matchdetail/timeline_block.py

from typing import Any, Dict, List


async def build_timeline_block(header: Dict[str, Any]) -> List[Dict[str, Any]]:
    """
    TODO: match_events, match_player_stats 등을 기반으로
    골/카드/교체 타임라인을 만들어서 리턴.

    1단계: 일단 빈 리스트를 리턴하고,
    나중에 실제 쿼리/매핑 로직을 채운다.
    """
    return []
